# Block private API keys and session tokens
auth.json

# Sessions are per-machine
sessions/

# No need for logs
pi-debug.log
.DS_Store

# Pi needs to stash its own binaries and installed packages sometimes.
bin
agent/npm/

# Offline caches of available models and MCP tools.
models-store.json
agent/mcp-cache.json

